'use strict';

angular
    .module('yourAppName.profile')
    .controller('ProfileController', ProfileController);

function ProfileController() {
    var vm = this;

    angular.extend(vm, {

    });
}
